# 담. — 개발 규칙

하루 사진 한 장에서 색을 추출해 그날을 색 하나로 기록하는 서비스.
구조: `apps/native` (Expo RN 쉘 + WebView) + `apps/web` (Next.js App Router, 실제 화면).

- **코드 스타일**: Airbnb JavaScript 스타일 가이드를 따른다. 위배되는 코드가 있으면 반드시 알려줄 것.
- **스타일링은 Emotion `css` prop**: `css={css\`...\`}` 인라인 방식을 기본으로 쓴다. Tailwind는 쓰지 않는다.
  - `styled` 컴포넌트는 기본 선택지가 아니다. 같은 스타일이 3곳 이상에서 반복되거나 재사용 가능한 UI 프리미티브(Button 등)를 만들 때만 쓴다.
  - `px` 고정값 사용을 최대한 피하고 `rem`, `em`, `vw`, `vh`, `dvh` 등 상대 단위를 사용한다. 위배 시 반드시 알려줄 것.
  - 색·간격 등 디자인 토큰은 `globals.css`의 CSS 변수로 정의하고 `var(--...)`로 참조한다.
  - **Emotion을 쓰는 파일은 반드시 클라이언트 컴포넌트(`'use client'`)여야 한다.**
  - `css` prop을 쓰는 파일은 **맨 첫 줄**에 `/** @jsxImportSource @emotion/react */`를, 그 다음 줄에 `'use client'`를 둔다. 순서가 바뀌면 TS가 pragma를 못 읽어 타입 에러가 난다.
  - **`tsconfig.json`에 `jsxImportSource`를 전역 설정하지 말 것.** 모든 파일의 JSX가 Emotion을 거치게 되어 서버 컴포넌트까지 오염되고, 빌드가 `createContext is not a function`으로 실패한다. 반드시 파일별 pragma로 제한한다.
- **쓸데없는 주석 금지**: 코드를 읽으면 알 수 있는 내용은 주석으로 쓰지 않는다.
  - 금지: 코드를 그대로 옮긴 설명(`// 상태를 초기화한다`), 함수/변수 이름의 반복, 구획 나누기 주석(`// ---- 핸들러 ----`), 주석 처리된 죽은 코드, TODO 없는 빈 껍데기 주석.
  - 허용: **왜** 그렇게 했는지가 코드에 안 드러나는 경우 — 라이브러리 버그 우회, 비직관적인 제약, 특정 순서를 지켜야 하는 이유, 의도적으로 감수한 트레이드오프.
  - 판단 기준: "이 주석을 지우면 다음 사람이 이걸 '정리'하다가 코드를 깨뜨릴까?" → 아니면 지운다.
  - 이름을 잘 지어서 주석이 필요 없게 만드는 쪽을 항상 우선한다.
- **관심사 분리 (SRP)**: UI와 도메인 로직을 최대한 분리한다. 단일 책임 원칙에 위배되는 코드가 있으면 반드시 알려줄 것.
- **iOS 우선**: 지금은 iOS만 지원한다. 안드로이드 전용 코드는 넣지 않는다.
