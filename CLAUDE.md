# 담. — 개발 규칙

하루 사진 한 장에서 색을 추출해 그날을 색 하나로 기록하는 서비스.
쌓인 색은 한 줄의 그라데이션이 되고, 둘이 한 방에서 나란히 채워간다.
구조: `apps/native` (Expo RN 쉘 — 유리 크롬) + `apps/web` (Next.js App Router — 콘텐츠).

기계가 판정할 수 있는 규칙은 이 문서가 아니라 `.claude/hooks/` 가 강제한다.
여기에는 그 바깥, 즉 **이 프로젝트가 무엇인지**와 **판단이 필요한 것**만 적는다.

## 정체성

- **스타일링은 Emotion `css` prop**: `css={css\`...\`}` 인라인 방식을 기본으로 쓴다.
  - `styled` 컴포넌트는 쓰지 않는다.
  - 색·간격 등 디자인 토큰은 `globals.css`의 CSS 변수로 정의하고 `var(--...)`로 참조한다.
  - 크기는 `rem`, `em`, `vw`, `vh`, `dvh` 등 상대 단위를 쓴다.
- **글자는 전부 갈무리(Galmuri14)**: 웹·네이티브 모두 같은 폰트를 쓴다. 새 화면·새 문구도 예외 없다.
  - **설계 크기 14px의 픽셀 폰트**다. 글자 크기는 14의 정수배(14, 28…)로 둬야 픽셀 격자가 또렷하다. 어중간한 크기는 뭉갠다.
  - **볼드 페이스가 없다.** `font-weight`를 올리면 브라우저가 합성 볼드를 만들어 픽셀을 뭉갠다. 위계는 굵기가 아니라 크기로 준다.
  - 웹은 `body`가 덮고 폼 요소는 `globals.css`가 `font: inherit`으로 붙잡는다. 네이티브는 `theme.js`의 `FONT`를 쓴다.
  - 탭바 문구만 예외다. 시스템이 그리는 크롬이라 애플 폰트를 따른다.
- **iOS 26 전용**: 리퀴드글래스가 iOS 26+ API라서 최소 버전을 26.0으로 잡았다. 그 아래는 설치되지 않으므로 유리 폴백 UI를 만들지 않는다. 안드로이드 전용 코드도 넣지 않는다.
- **코드 스타일**: Airbnb JavaScript 스타일 가이드를 따른다.

## 화면 경계 — 크롬은 네이티브, 콘텐츠는 웹

CSS로는 리퀴드글래스를 흉내낼 수 없다. 굴절·광원·터치 변형이 전부 `UIGlassEffect`의 몫이라서,
**떠 있는 것은 전부 네이티브**로 그린다.

| 어디 | 무엇 |
| ---- | ---- |
| 네이티브 | 탭바 — `expo-router`의 `NativeTabs` (실제 `UITabBarController`) |
| 웹 | 그 사이 콘텐츠 전부 — 방울, 그라데이션, 사진, 메모 |

- **유리를 직접 그리지 말 것.** 시스템 탭바는 iOS 26에서 애플이 리퀴드글래스로 그려 주고, 스크롤 축소·접근성 대응·다음 OS의 디자인 변경까지 따라온다. 손으로 만든 유리는 그중 무엇도 받지 못한다.
- 그래서 `expo-glass-effect`·`react-native-reanimated`·`react-native-gesture-handler`를 쓰지 않는다. 시스템이 안 그려 주는 요소(플로팅 버튼 등)가 생기면 그때 다시 판단한다.
- 햅틱은 웹에서 못 울린다(`navigator.vibrate` 미지원). 브릿지로 네이티브에 요청해 `expo-haptics`가 울린다.
- 크롬을 웹에서 CSS로 흉내내지 말 것. `backdrop-filter`로 만든 가짜 유리는 진짜 유리 옆에 놓이는 순간 티가 난다.

## 색

- 배경은 라이트에서 흰색, 다크에서 검정. 기록한 색이 주인공이므로 배경은 색을 갖지 않는다.
- 선택된 탭 색도 **무채색**이다. 크롬에 의미 없는 색(iOS 기본 파랑 등)을 두면 방울과 경쟁한다.
- 네이티브 `apps/native/theme.js`와 웹 `globals.css`의 배경값은 **같아야 한다.** 웹이 뜨기 전과 safe area 바깥을 네이티브가 채우므로, 어긋나면 경계에서 화면이 끊긴다.

## 테스트 경계

- **로직은 테스트를 먼저 쓴다**: `apps/*/lib/`, `apps/*/utils/` 아래의 순수 함수, 파서, 변환, 상태 전이.
- **화면은 테스트하지 않는다**: `apps/web/src/app/**/*.tsx`. 스타일 값을 그대로 옮긴 테스트는 버그를 잡지 못하면서 디자인을 바꿀 때마다 같이 고쳐야 하는 부채가 된다.
- 테스트는 프레임워크 없이 `node:test` + `node:assert`로 쓰고 `tsx --test`로 돌린다.
- 테스트 파일은 글롭으로 자동 수집되므로 새로 만들면 별도 등록이 필요 없다.

## 판단이 필요한 것

- **라우팅의 진실은 웹에만 있다**: 크롬이 네이티브로 나가면서 화면 상태가 두 군데 생길 수 있다. 네이티브는 **그리기만** 한다. 뒤로가기·탭 전환은 "눌렸다"는 사실만 웹에 보내고, 어디로 갈지는 웹이 정한 뒤 결과를 네이티브에 알려 준다. 네이티브가 자기 판단으로 화면을 바꾸면 두 상태가 어긋나 뒤로가기가 엉뚱한 곳으로 간다.
- **관심사 분리 (SRP)**: UI와 도메인 로직을 분리한다. 색 계산·날짜 처리·데이터 변환이 컴포넌트 안에 있으면 `lib/`으로 뺀다. 위배되는 코드가 있으면 반드시 알려줄 것.
- **쓸데없는 주석 금지**: 코드를 읽으면 알 수 있는 내용은 쓰지 않는다.
  - 금지: 코드를 그대로 옮긴 설명, 함수/변수 이름의 반복, 구획 나누기 주석, 주석 처리된 죽은 코드.
  - 허용: **왜** 그렇게 했는지가 코드에 안 드러나는 경우 — 라이브러리 버그 우회, 비직관적인 제약, 특정 순서를 지켜야 하는 이유, 의도적으로 감수한 트레이드오프.
  - 판단 기준: "이 주석을 지우면 다음 사람이 이걸 '정리'하다가 코드를 깨뜨릴까?" → 아니면 지운다.
  - 이름을 잘 지어서 주석이 필요 없게 만드는 쪽을 항상 우선한다.

## 검증 (자동)

세 층이 순서대로 돈다. 아래 규칙들은 이 문서가 아니라 hook이 강제하므로, 잊더라도 통과할 수 없다.

| 층                        | 시점           | 검사                                                                                                 | 실패 시                              |
| ------------------------- | -------------- | ---------------------------------------------------------------------------------------------------- | ------------------------------------ |
| L1 `verify-syntax.sh`     | 파일 저장마다  | Emotion pragma 순서, `'use client'` 누락, 불필요한 pragma, `tsconfig`의 `jsxImportSource`, px 고정값 | 지적이 컨텍스트로 주입됨 (차단 없음) |
| L2 `verify-regression.sh` | 작업을 끝낼 때 | typecheck, test, lint                                                                                | **종료 차단**                        |
| L3 prompt hook            | L2 통과 후     | 테스트 커버리지 누락, SRP, 주석, UI 품질, 요청 충족                                                  | 종료 차단                            |
| CI                        | push / PR      | 위 전부 + `web build`                                                                                | PR 빨강                              |

L1이 차단하지 않는 이유: 작업 도중 일시적으로 깨진 상태는 정상이다. 끝낼 때만 초록이면 된다.

### Emotion 파일 규칙 (L1이 검사)

- `css` prop을 쓰는 파일은 **맨 첫 줄**에 `/** @jsxImportSource @emotion/react */`를 둔다. pragma는 파일 맨 앞에서만 인식되므로 순서가 바뀌면 스타일이 적용되지 않는다.
- Emotion을 쓰는 파일은 반드시 클라이언트 컴포넌트(`'use client'`)여야 한다.
- **`tsconfig.json`에 `jsxImportSource`를 전역 설정하지 말 것.** 서버 컴포넌트까지 오염되어 빌드가 `createContext is not a function`으로 실패한다. 반드시 파일별 pragma로 제한한다.
